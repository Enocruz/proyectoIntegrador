@model IEnumerable<ProyectoIntegrador.Models.HISTORIAL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MOVIMIENTO.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SUBCASO.DESCRIPCION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.USUARIO.Apellido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MOVIMIENTO.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUBCASO.DESCRIPCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIO.Apellido)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idHistorial }) |
            @Html.ActionLink("Details", "Details", new { id=item.idHistorial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idHistorial })
        </td>
    </tr>
}

</table>
